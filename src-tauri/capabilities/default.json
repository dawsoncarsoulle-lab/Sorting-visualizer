{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main SortFlow window",
  "windows": ["main"],
  "permissions": ["core:default"]
}

